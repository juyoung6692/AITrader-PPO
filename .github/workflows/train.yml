name: Train PPO Scalper (fast)

on:
  workflow_dispatch:
    inputs:
      symbols:
        description: "Space-separated tickers"
        default: "AAPL MSFT NVDA TSLA AMD"
      interval:
        description: "Bar interval (5m / 15m / 1h)"
        default: "5m"
      period:
        description: "Lookback (60d / 730d, etc.)"
        default: "60d"
      timesteps:
        description: "PPO timesteps for this run"
        default: "500000"
      n_envs:
        description: "Parallel envs"
        default: "4"
      data_source:
        description: "yfinance | alpaca"
        default: "yfinance"
  schedule:
    - cron: "0 * * * *"

permissions:
  contents: write

concurrency:
  group: ppo-train
  cancel-in-progress: false

jobs:
  train:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Date stamp (cache key)
        id: date
        run: echo "value=$(date -u +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Cache market data
        uses: actions/cache@v4
        with:
          path: data_cache
          key: ${{ github.event.inputs.data_source || 'yfinance' }}-${{ github.event.inputs.interval || '5m' }}-${{ github.event.inputs.period || '60d' }}-${{ steps.date.outputs.value }}
          restore-keys: |
            ${{ github.event.inputs.data_source || 'yfinance' }}-${{ github.event.inputs.interval || '5m' }}-${{ github.event.inputs.period || '60d' }}-

      - name: Train (resume if model exists)
        env:
          ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
          ALPACA_API_SECRET: ${{ secrets.ALPACA_API_SECRET }}
          DATA_SOURCE: ${{ github.event.inputs.data_source || 'yfinance' }}
        run: |
          python -m ppo_trader.scalping_train \
            --symbols ${{ github.event.inputs.symbols || 'AAPL MSFT NVDA TSLA AMD' }} \
            --interval ${{ github.event.inputs.interval || '5m' }} \
            --period ${{ github.event.inputs.period || '60d' }} \
            --timesteps ${{ github.event.inputs.timesteps || '500000' }} \
            --n-envs ${{ github.event.inputs.n_envs || '4' }} \
            --save-path models/ppo_scalper \
            | tee scalping_report.txt

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ppo-scalper-${{ github.run_number }}
          path: |
            models/ppo_scalper.zip
            scalping_report.txt
          retention-days: 14

      - name: Commit updated model
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add models/ppo_scalper.zip
          git diff --cached --quiet && echo "no model changes" || git commit -m "chore: continual scalper retrain (fast) [skip ci]"
          git push || true
